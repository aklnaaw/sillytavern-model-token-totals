# 版本规则

自用插件。

## 规矩

- **改样式 / 修 bug / 小调整** → 只动补丁号：`0.5.1` → `0.5.2` → `0.5.3`
- **新增功能** → 才动次版本号（`0.5.x` → `0.6.0`），且需先确认
- **补丁级也发 Release**（检查更新按钮靠 Release 才能查到新版本）
- Release 说明**只写更新了什么**，一两句话，不写安装/用法/背景
- 安装和用法统一放 README，不重复写进 Release

## Release 说明格式

只列改动，例如：

    - 聊天记录按角色分组，同名聊天带编号
    - 时间精确到分钟

## 当前

最新版本：`v0.5.1`
