# 版本规则

自用插件，版本号别刷太勤。

## 规矩

- **改样式 / 修 bug / 小调整** → 只动补丁号：`0.5.0` → `0.5.1` → `0.5.2`
- **新增功能** → 才动次版本号（`0.5.x` → `0.6.0`），且需先确认
- 补丁级改动**不单独发 Release**，直接推 `main`
- 攒够一批值得说的改动，或用户明确要求时，才发一次 Release

## 当前

最新发布版本：`v0.5.0`

## 一条命令更新

（git 安装的话）扩展面板 → 检查更新，或重下 zip 覆盖。
